# Changelog

## v1.4.0

- add `twitterCard` to `PageSeoData` to set `twitter:card` meta tag
- Update `presetsLoader` to accept laoders as well as `SeoData`

## v1.1.0

- add Loaders support for @ngaox/seo
- add `ng g @ngaox/seo:setup` for an easy `SeoModule` setup in any module

## v1.0.0

- Add `SeoService` for setting document meta tags & title
- Add `SeoModule` to set `SeoService` page seoData default values
