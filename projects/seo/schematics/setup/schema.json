{
    "$schema": "http://json-schema.org/schema",
    "id": "setup",
    "title": "Easy setup to get started with Ngaox-Seo in a specific Module",
    "type": "object",
    "properties": {
        "name": {
            "description": "The name of the seo module",
            "type": "string"
        },
        "inline": {
            "description": "whather ot not including all configs in one file",
            "type": "boolean",
            "default": "false"
        },
        "project": {
            "type": "string",
            "description": "The targeted project",
            "$default": {
                "$source": "projectName"
            }
        },
        "module": {
            "type": "string",
            "description": "The module that will import the generated Seo-Module"
        }
    },
    "required": []
}